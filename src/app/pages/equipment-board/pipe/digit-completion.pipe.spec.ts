import { DigitCompletionPipe } from './digit-completion.pipe';

describe('DigitCompletionPipe', () => {
  it('create an instance', () => {
    const pipe = new DigitCompletionPipe();
    expect(pipe).toBeTruthy();
  });
});
